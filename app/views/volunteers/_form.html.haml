= form_for [@volunteerable, @events, @items, @volunteer] do |f|
	= f.label :name
	= f.text_field :name	
	= f.label :quantity_given
	= f.text_field :quantity_given
	= f.submit
= link_to 'cancel', '#' ,class: 'cancel-volunteer'