= form_for [@user, @event], :html => {:multipart => true}, remote: true do |f|
	%h3 Create Event
	= f.label :name
	= f.text_field :name
	= f.label :time
	= f.text_field :time
	= f.label :date
	= f.text_field :date
	%br
	= f.submit 'Create Event', class: 'btn-large btn-primary create-event'
	