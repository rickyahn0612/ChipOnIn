.item-add-popup
	= form_for [@itemable, @eventable, @item], remote: true do |f|
		= f.label :name
		= f.text_field :name
		= f.label :quantity_needed
		= f.text_field :quantity_needed
		= f.submit "Add Items", class: 'btn-large btn-success', style: 'padding-bottom: 30px !important;'